version: '3.8'

services:
  nuxt-app:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    container_name: nuxt-notes-app
    ports:
      - "3000:3000" 
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
    restart: unless-stopped
